spring.datasource.url=jdbc:postgresql://ep-holy-paper-ayermtkj-pooler.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=neondb_owner
spring.datasource.password=npg_hPzNWk1pD2Us

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

server.port=${PORT:8080}

# Per-IP fixed-window limits. Kafka publishing is opt-in, so deployments without
# a Kafka broker continue to start normally.
app.rate-limit.enabled=true
app.rate-limit.window-seconds=60
app.rate-limit.default-requests=120
app.rate-limit.course-requests=60
app.rate-limit.auth-requests=10
app.rate-limit.trust-forwarded-headers=true
app.rate-limit.kafka.enabled=${RATE_LIMIT_KAFKA_ENABLED:false}
app.rate-limit.kafka.topic=${RATE_LIMIT_KAFKA_TOPIC:openlecture.rate-limit-events}
